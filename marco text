ti = getTitle();
run("Lab Stack");
run("Stack to Images");
selectWindow("b*");
close();
selectWindow("L*");
close();
selectWindow("a*");
rename(ti);
run("8-bit");
setThreshold(X, 255);
setOption("BlackBackground", true);
run("Convert to Mask");
run("Make Binary");
run("Set Measurements...", "area center perimeter bounding shape area_fraction display redirect=None decimal=4");
run("Set Scale...", "distance=Y known=10 pixel=1 unit=cm global");
run("Analyze Particles...", "size=0-Infinity show=Masks display summarize");
selectWindow(ti);
close();
selectWindow("Mask of "+ti);
rename(ti);
